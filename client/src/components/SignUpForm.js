
// on Submit 
// assign name
// WRITE to database: random generated name 'rgName', username, and password!

// if find_all.name matches name or find_all.email matches then say this name is taken or this email 
// is already being used. try a diff one

import React from "react";


export default function SignUpForm() {
    return ( 
        <div className = "about"> 
       <p> Yeah so like bother your coworkers. This goes out to all the ppl on their phones on the toilet! </p>
        </div>
    )
}